# 환경 변수 예시 파일
# 실제 사용 시 이 파일을 복사하여 .env 파일로 저장하세요.
# cp .env.example .env

# 앱 설정
APP_NAME=농업 기상 데이터 API
APP_VERSION=1.0.0
DEBUG=false

# 데이터베이스 설정
DB_HOST=localhost
DB_PORT=5432
DB_NAME=ag_weather_db
DB_USER=smartfarm
DB_PASSWORD=smartfarm

# CORS 설정 (콤마로 구분, 예: http://localhost:3000,http://example.com)
CORS_ORIGINS=*

# 페이지네이션 기본값
DEFAULT_PAGE_SIZE=20
MAX_PAGE_SIZE=100
